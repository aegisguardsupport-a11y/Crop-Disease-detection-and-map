# Development dependencies. Always installs runtime deps too via -r.
-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
invoke>=2.2,<3.0

# Phase 2 — API integration tests use FastAPI TestClient (httpx-backed)
httpx>=0.27,<1.0
